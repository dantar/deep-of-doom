import { MobContainerDirective } from './mob-container.directive';

describe('MobContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new MobContainerDirective();
    expect(directive).toBeTruthy();
  });
});
